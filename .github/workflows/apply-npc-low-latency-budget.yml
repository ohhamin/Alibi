name: Apply NPC low latency budget

on:
  push:
    branches:
      - master
    paths:
      - 'backend/tool/apply_npc_low_latency_budget.py'
      - '.github/workflows/apply-npc-low-latency-budget.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - name: Patch and validate source
        run: |
          python backend/tool/apply_npc_low_latency_budget.py
          git diff --check
          python -m compileall -q backend/app
      - name: Commit patched source
        run: |
          if git diff --quiet -- backend/app/agent_service.py backend/app/game_service.py; then
            echo 'No source changes needed.'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add backend/app/agent_service.py backend/app/game_service.py
          git commit -m 'Lower NPC token budgets for faster turns'
          git push origin HEAD:master
